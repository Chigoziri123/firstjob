name: testing pipeline

on:
    push:
        branches:
            - main

jobs:
    test1:
        runs-on: ubuntu-latest
        steps:
            - 
              name: say hello world
              run: echo "Say hello world. This is our first pipeline."
